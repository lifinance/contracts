// SPDX-License-Identifier: LGPL-3.0-only
pragma solidity ^0.8.17;

import { UpdateScriptBase } from "./utils/UpdateScriptBase.sol";

contract DeployScript is UpdateScriptBase {
    function run()
        public
        returns (address[] memory facets, bytes memory cutData)
    {
        return update("{{properCase name}}Facet");
    }

    // If you need to exclude selectors from the diamond cut (e.g. init functions
    // or selectors that clash with other facets), import the facet
    // (import { {{properCase name}}Facet } from "lifi/Facets/{{properCase name}}Facet.sol";)
    // and override getExcludes():
    //
    // function getExcludes() internal pure override returns (bytes4[] memory) {
    //     bytes4[] memory excludes = new bytes4[](1);
    //     excludes[0] = {{properCase name}}Facet.init{{properCase name}}.selector;
    //
    //     return excludes;
    // }
    //
    // If your facet needs initialization calldata that is executed when adding
    // the facet to the diamond, override getCallData():
    //
    // function getCallData() internal override returns (bytes memory) {
    //     path = string.concat(root, "/config/{{kebabCase name}}.json");
    //     json = vm.readFile(path);
    //
    //     return
    //         abi.encodeWithSelector(
    //             {{properCase name}}Facet.init{{properCase name}}.selector,
    //             json.readAddress(string.concat(".example.", network))
    //         );
    // }
}
