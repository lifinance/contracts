# Copy this file to `.env.mcp.local` (repo root) and fill in values.
# `.env.mcp.local` is gitignored by default.

# -----------------------------------------------------------------------------
# RPC
# -----------------------------------------------------------------------------
# Used by the Foundry MCP server for `cast_call`.
RPC_URL=

# -----------------------------------------------------------------------------
# GitHub MCP (ghcr.io/github/github-mcp-server)
# -----------------------------------------------------------------------------
# Create a GitHub Personal Access Token (PAT) with repo access for the repos you need.
GITHUB_PERSONAL_ACCESS_TOKEN=

# -----------------------------------------------------------------------------
# Jira MCP (@aashari/mcp-server-atlassian-jira)
# -----------------------------------------------------------------------------
# Example site: `your-company` for `https://your-company.atlassian.net`
ATLASSIAN_SITE_NAME=
ATLASSIAN_USER_EMAIL=
ATLASSIAN_API_TOKEN=

# -----------------------------------------------------------------------------
# Notion MCP (@ramidecodes/mcp-server-notion)
# -----------------------------------------------------------------------------
NOTION_API_KEY=

# -----------------------------------------------------------------------------
# Tenderly MCP (repo-owned server)
# -----------------------------------------------------------------------------
TENDERLY_ACCESS_KEY=
TENDERLY_ACCOUNT=
TENDERLY_PROJECT=

# -----------------------------------------------------------------------------
# Explorer MCP (repo-owned server; Etherscan-style API, often Blockscout-compatible)
# -----------------------------------------------------------------------------
# By default, the explorer MCP server uses `config/networks.json` → `explorerApiUrl` and (if available)
# the corresponding API key env var from `foundry.toml`'s [etherscan] section.
#
# Optional: set a default network key (e.g. "mainnet", "base", "arbitrum") to avoid passing it per tool call.
EXPLORER_NETWORK=
#
# Advanced overrides (only if you want to force a single explorer API endpoint):
EXPLORER_API_BASE_URL=
EXPLORER_API_KEY=
EXPLORER_KIND=

# -----------------------------------------------------------------------------
# Slack MCP (@modelcontextprotocol/server-slack)
# -----------------------------------------------------------------------------
SLACK_BOT_TOKEN=
SLACK_TEAM_ID=
