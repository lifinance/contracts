# Contract Changelogs

This directory holds auto-generated changelogs for Solidity contract changes.

## File structure

- **`CHANGELOG.md`** – Single file with all contract changes by commit (newest first).
- **`contracts/{ContractName}.md`** – Per-contract history (e.g. `contracts/DiamondLoupeFacet.md`).

## Format

Each entry includes:

- **Commit**: hash, date, author, link to GitHub
- **Categories**: ⚠️ Breaking Changes, ✨ Added, 🔄 Changed, 🗑️ Removed, 🐛 Fixed

## When changelogs are generated

Changelogs are generated by the **GitHub Actions workflow** when:

- **Push to any branch** that modifies files under `src/**/*.sol`
- **Manual run** via Actions tab → `generateContractChangelog` → Run workflow

Only **real code changes** trigger generation: comment-only or NatSpec-only diffs (including `@custom:version` bumps) are skipped to avoid unnecessary runs and API cost.

## Related files

- Workflow: `.github/workflows/generateContractChangelog.yml`
- Script: `script/changelogAnalysis/generateContractChangelog.ts`

Analysis uses **Claude Sonnet (Anthropic API)**; requires `CLAUDE_CODE_SC_CONTRACTS_REPO_CHANGELOGS_API_KEY`.
