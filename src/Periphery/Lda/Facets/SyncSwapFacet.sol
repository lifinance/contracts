


// src/Periphery/Lda/Facets/SyncSwapFacet.sol
contract SyncSwapFacet {
    using LibInputStream for uint256;

    function swapSyncSwap(
        uint256 stream,
        address from,
        address tokenIn,
        uint256 amountIn
    ) external returns (uint256) {
        // Move SyncSwap logic here
    }
}