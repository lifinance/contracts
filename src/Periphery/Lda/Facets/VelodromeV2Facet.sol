// src/Periphery/Lda/Facets/VelodromeV2Facet.sol
contract VelodromeV2Facet {
    using LibInputStream for uint256;

    function swapVelodromeV2(
        uint256 stream,
        address from,
        address tokenIn,
        uint256 amountIn
    ) external returns (uint256) {
        // Move Velodrome V2 swap logic here
    }
}